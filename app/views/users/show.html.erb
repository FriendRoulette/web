<%= render partial: 'appheader' %>
<section class="intro">
    <div class="intro-body">
    	<div class="background-shade">
			<h1><%= @user.name %></h1>	
			<p><%= @user.friends.count %> Active Friends</p>
			<p><%= @friends.count %> Friends</p>	
		</div>
	</div>
</section>